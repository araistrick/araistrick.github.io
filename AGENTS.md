# Repository workflow

- The deployment branch is `master`.
- Commit and push changes directly to `origin/master`.
- Do not create feature branches or pull requests in this repository.
- GitHub Pages deploys automatically from `master` through `.github/workflows/deploy.yml`.
